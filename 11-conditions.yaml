- name : conditions
  hosts: web
  become: yes
  tasks:
    - name: check user exits or not
      ansible.builtin.command: id expense

    - name: say hello
      ansible.builtin.debug:
      msg: "Hello"